import _extends from 'babel-runtime/helpers/extends';
import _objectWithoutProperties from 'babel-runtime/helpers/objectWithoutProperties';
import React from 'react';
import classnames from 'classnames';
var Icon = React.createClass({
    displayName: 'Icon',

    propTypes: {
        component: React.PropTypes.node,
        classPrefix: React.PropTypes.string,
        name: React.PropTypes.string.isRequired
    },
    getDefaultProps: function getDefaultProps() {
        return {
            classPrefix: 'iconfont',
            component: 'i'
        };
    },
    render: function render() {
        var _classnames;

        var _props = this.props;
        var classPrefix = _props.classPrefix;
        var Component = _props.component;
        var name = _props.name;

        var props = _objectWithoutProperties(_props, ['classPrefix', 'component', 'name']);

        var iconClass = classnames(classPrefix, (_classnames = {}, _classnames[classPrefix + '-' + name] = name, _classnames));
        return React.createElement(Component, _extends({ className: iconClass }, props));
    }
});

export default Icon;