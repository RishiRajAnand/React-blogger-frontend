import _extends from 'babel-runtime/helpers/extends';
import _objectWithoutProperties from 'babel-runtime/helpers/objectWithoutProperties';
/** 
 * @file Col used with Grid to build grid system
 * @author mayingcong <mayingcong@xingxy.cn>
 * @date   2016-11-01
 */
import React from 'react';
import classnames from 'classnames';

var Col = React.createClass({
    displayName: 'Col',

    propTypes: {
        cols: React.PropTypes.oneOf([1, 2, 3, 4, 5, 6]),
        offset: React.PropTypes.oneOf([1, 2, 3, 4, 5, 6])
    },
    render: function render() {
        var _classnames;

        var _props = this.props;
        var cols = _props.cols;
        var offset = _props.offset;

        var props = _objectWithoutProperties(_props, ['cols', 'offset']);

        cols = cols ? cols > 6 ? 6 : cols : 0;
        offset = offset ? offset > 6 ? 6 : offset : 0;
        var colClass = classnames('col', (_classnames = {}, _classnames['col-' + cols] = cols, _classnames['col-offset-' + offset] = offset, _classnames));
        return React.createElement('div', _extends({ className: colClass }, props));
    }
});

export default Col;